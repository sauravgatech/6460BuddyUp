@model IEnumerable<GT.CS6460.BuddyUp.WebAPP.Models.Question>
<div id="questionRows">
    @foreach (var item in Model)
    {
        Html.RenderPartial("_CourseQuestion", item);
    }
</div>
@Html.ActionLink("Add another...", "BlankQuestionRow", null, new { id = "addQuestion" })
